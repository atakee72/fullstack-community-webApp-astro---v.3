---
// Logout page - simple redirect (Better Auth handles session clearing)
// Better Auth's API endpoint already handles session clearing through authClient.signOut()
// This page is just a fallback redirect
return Astro.redirect('/');
---
