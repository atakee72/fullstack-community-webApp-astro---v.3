---
// Logout page - simple redirect (NextAuth handles session clearing)
// NextAuth's API endpoint already handles session clearing through signOut()
// This page is just a fallback redirect
return Astro.redirect('/');
---
