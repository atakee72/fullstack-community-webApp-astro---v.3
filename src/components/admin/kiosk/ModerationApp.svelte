<script lang="ts">
  // Shell only — Task 1 lays the foundation (AdminLayout + role gate).
  // Tasks 3–9 replace this file's body incrementally with the real queue,
  // stat row, filters, and review actions.
  let { adminName }: { adminName: string } = $props();
</script>

<div class="font-dmmono" style="padding:36px; color:var(--k-ink-mute);">wird geladen…</div>
