---
import BaseLayout from '../layouts/BaseLayout.astro';
import RegisterForm from '../components/RegisterForm';
---

<BaseLayout title="Register">
  <main class="min-h-[calc(100vh-80px)] flex items-center justify-center bg-gradient-to-br from-[#eccc6e] to-[#dabb5d] py-8">
    <div class="max-w-md w-full mx-4">
      {/* Logo Section */}
      <div class="text-center mb-8">
        <div class="inline-block bg-white rounded-full p-4 shadow-lg mb-4">
          <span class="text-5xl">🤝</span>
        </div>
        <h2 class="text-3xl font-bold text-[#814256]">Join Our Community!</h2>
        <p class="text-gray-700 mt-2">Create your Mahalle account</p>
      </div>

      {/* Register Card */}
      <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
        <div class="bg-gradient-to-r from-[#814256] to-[#6a3646] p-6">
          <h1 class="text-2xl font-bold text-white text-center">Sign Up</h1>
        </div>

        <div class="p-8">
          <RegisterForm client:load />

          <div class="mt-8 pt-6 border-t border-gray-200 text-center">
            <p class="text-gray-600">
              Already have an account?{" "}
              <a href="/login" class="text-[#4b9aaa] font-semibold hover:text-[#814256] transition-colors">
                Login here
              </a>
            </p>
          </div>
        </div>
      </div>

      {/* Footer Text */}
      <p class="text-center text-gray-700 mt-8 text-sm">
        By signing up, you agree to our Terms of Service and Privacy Policy
      </p>
    </div>
  </main>
</BaseLayout>